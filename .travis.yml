sudo: false
language: python
python:
    - "2.7"
    - "3.6"
install: pip install tox-travis coverage pytest
script:
    - tox
    - coverage run -m pytest wlmodem
    - coverage report -m
after_success:
  - coveralls
